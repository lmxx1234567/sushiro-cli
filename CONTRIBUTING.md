# 贡献指南

欢迎改进公共查询、错误处理、文档与测试。CLI 和 MCP 共用 service，公共查询必须保持与个人会话隔离。请先阅读 [开发与测试](docs/development.md) 及 [支持范围](docs/limitations.md)。

提交 PR 时说明具体问题、修改后的行为、验证结果与未验证范围。协议、配置隔离与错误处理变化应有必要的回归测试；文档小改无需添加镜像实现的测试。

测试使用虚构凭证与业务数据。不要提交真实 token、手机号、WechatID、个人票据、原始请求/响应、抓包、私钥或个人机器路径。内置公共默认值是限定于现有源文件的例外，不应复制到日志、issue、文档或测试。`.gitignore` 不会移除已跟踪文件或 Git 历史；提交前检查差异和署名信息。

默认测试不调用线上业务接口。任何个人请求或创建/取消预约均需账号持有人对具体操作的授权；不为测试占用名额，也不自动重试结果不确定的写请求。反馈只附脱敏错误码、平台、版本与最小复现；敏感问题遵循 [SECURITY](SECURITY.md)。

复用代码须保留作者、固定来源和许可，更新 [来源说明](docs/provenance.md) 与第三方声明。依赖或工具链变更需重新检查许可证覆盖。发布条件见 [发布说明](docs/releases.md)。
